# Spreadsheet
## Development
### In PyCharm
Mark `src/` as sources root and `tests/` as test sources root.
### Requirements
You can install them with
```
$ pip install -e .
```
or
```
$ pip install -e .[tests]
```
